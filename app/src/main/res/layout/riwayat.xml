<?xml version="1.0" encoding="utf-8"?>
<!-- Root layout using ConstraintLayout for flexible positioning -->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layout_absence_history"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/custom_blue"
    android:fitsSystemWindows="true"
    tools:context=".MainActivity">

    <!-- Top Blue Header -->

    <View
        android:id="@+id/header_background"
        android:layout_width="435dp"
        android:layout_height="206dp"
        android:background="@color/custom_blue"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/month_navigation"
        android:layout_width="377dp"
        android:layout_height="85dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="24dp"
        android:layout_marginEnd="16dp"
        android:background="@drawable/riwayat_rounded_white_background"
        android:elevation="4dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Left Arrow -->
        <ImageView
            android:id="@+id/btn_prev_month"
            android:layout_width="51dp"
            android:layout_height="51dp"
            android:layout_marginStart="16dp"
            android:contentDescription="previous_month"
            android:src="@drawable/riwayat_arrow_left"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Background untuk bulan (SEKARANG DIBAWAH TEXT) -->

        <!-- Month Label (TERIKAT KE BACKGROUND) -->
        <View
            android:id="@+id/month_bg"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:background="@drawable/riwayat_rounded_blue_bg"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/btn_next_month"
            app:layout_constraintHorizontal_bias="0.0"
            app:layout_constraintStart_toEndOf="@id/btn_prev_month"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintVertical_bias="0.488" />

        <TextView
            android:id="@+id/tv_month"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingHorizontal="16dp"
            android:paddingVertical="8dp"
            android:text="Desember"
            android:textColor="@color/white"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="@id/month_bg"
            app:layout_constraintEnd_toEndOf="@id/month_bg"
            app:layout_constraintStart_toStartOf="@id/month_bg"
            app:layout_constraintTop_toTopOf="@id/month_bg" />

        <!-- Right Arrow -->
        <ImageView
            android:id="@+id/btn_next_month"
            android:layout_width="51dp"
            android:layout_height="51dp"
            android:layout_marginEnd="16dp"
            android:contentDescription="next_month"
            android:src="@drawable/riwayat_arrow_right"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />
    </androidx.constraintlayout.widget.ConstraintLayout>


    <!-- Fragment for Attendance List (scrollable) -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/attendance_fragment_container"
        android:name="com.example.epresent_prg6.AttendanceListFragment"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/rounded_top_background"
        app:layout_constraintTop_toBottomOf="@id/month_navigation"
        app:layout_constraintBottom_toTopOf="@id/bottom_navigation"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Bottom Navigation Bar -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:background="@color/white"
        app:menu="@menu/bottom_nav_menu"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:padding="5dp"
        />
    </androidx.constraintlayout.widget.ConstraintLayout>
